{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}

{% set path = '/' ~ value %}

{% if value %}
    <img src="{{ path | apply_filter('small_thumb') }}" border="0" alt="{{object.alt}}" title="{{object.title}}" />
{% endif %}

{% endblock %}